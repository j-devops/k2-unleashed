# K2 Pro Network Deployment Configuration
# Copy this file to .env and fill in your printer details

# Printer connection details
PRINTER_HOST=192.168.1.113           # Printer IP address or hostname
PRINTER_USER=root                     # SSH username (default: root)
PRINTER_PORT=22                       # SSH port (usually 22)

# SSH Authentication
# Option 1: Use SSH key (recommended - no password needed)
# Make sure your SSH key is already set up: ssh-copy-id root@printer-ip
# Option 2: Use password (default Creality password shown below)
PRINTER_PASSWORD=creality2024        # Default Creality password 

# K2 Model Selection
# 1 = K2 Plus (F008) - 350×350×350mm, Dual Z
# 2 = K2 Pro (F012) - 300×300×300mm, Single Z
# 3 = K2 Base (F021) - 260×260×260mm, Single Z
K2_MODEL=2

# Deployment Options
# 1 = System Monitor only (minimal)
# 2 = System Monitor + Diagnostics (recommended)
# 3 = Everything (monitor, diagnostics, web dashboard)
DEPLOY_CHOICE=3

# Optional: Auto-confirm deployment (skip confirmation prompt)
# Set to 'yes' to skip the final confirmation prompt
AUTO_CONFIRM=yes
